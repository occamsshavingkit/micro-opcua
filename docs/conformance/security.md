# Conformance: Security

**WARNING**: This minimal server initially targets `SecurityPolicy - None`. 

- **Non-Production Use Only**: Endpoints using `SecurityPolicy - None` should only be used in trusted, isolated networks or for testing.
- **Crypto-Pluggability**: The architecture is designed to allow plugging in a cryptographic backend (e.g., mbedTLS, BearSSL) in the future.
